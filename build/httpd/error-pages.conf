# Because the path to error pages is relative to the DocumentRoot,
# and because we have a VirtualDocumentRoot, we must specify an
# alias to be able to have global error pages (for all vhosts).
Alias /global-error /var/www/error

# 403
ErrorDocument 403 /global-error/403.php
